pipeline {
    agent any
    stages {
        stage('Example Build') {
            steps {
                echo 'Hello sub build'
            }
        }
        stage('Example Test') {
            steps {
                echo 'Testing sub build'
            }
        }
    }
}
